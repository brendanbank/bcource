services:
  bcourse: # Define a service named 'webapp'

    build: ..
    ports:
      - "8081:8080" # Map port 8000 on the host to port 8000 in the container
    env_file: ".env"
    networks:
      - external-db-network

networks:
  external-db-network:
    driver: bridge

